# Congress.gov API key
CONGRESS_API_KEY=your_congress_api_key_here

# X API v2 credentials — OAuth 1.0a user context
X_API_KEY=your_api_key_here
X_API_SECRET=your_api_secret_here
X_ACCESS_TOKEN=your_access_token_here
X_ACCESS_SECRET=your_access_token_secret_here

# How often to check (minutes). Default 15 locally; Render uses 5 via render.yaml
POLL_INTERVAL_MINUTES=15

# Directory for posted.json state file. Set to /data on Render (persistent disk mount)
DATA_DIR=

# Set to "true" to log posts without posting (safe for testing)
DRY_RUN=true

# Set to "false" to skip image generation and post text-only (required on X Free tier)
VOTE_IMAGE=true
